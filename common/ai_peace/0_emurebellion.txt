# Logic behaviour that the Australian and Emu Rebellion AI will fallback on if they are in a civil war
# Should focus on getting the nation united again no matter their ideology
# Written by Henrik "Groogy" Hansson, edited by King Emu I and Rod Hull

eme_civilwar_peace = {
	# ROOT = Taker, FROM = Giver
	enable = {
		AST = {
		    has_war_with = EME
			has_wargoal_against = EME
		}
		OR = {
		    ENG = {
			    has_war_with = EME
				is_in_faction_with = AST
			}
			any_country = {
			    limit = { is_in_faction_with = AST }
				has_war_with = EME
			}
		}
	}

	annex_randomness = 0
	liberate_randomness = 0
	puppet_randomness = 0
	take_states_randomness = 0
	force_government_randomness = 0
	
	# ROOT = Taker, FROM = Giver
	annex = {
		factor = 1000
	}

	# ROOT = Taker, FROM = Liberated
	liberate = {
		factor = 0
	}

	# ROOT = Taker, FROM = Giver
	puppet = {
		factor = 0
	}

	# ROOT = Taker, FROM = State
	take_states = {
		factor = 0
	}

	# ROOT = Taker, FROM = Giver
	force_government = {
		factor = 0
	}
}
